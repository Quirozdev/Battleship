import GameBoard from './gameBoard';

export default function Computer(computerName) {
  const name = computerName;
  const gameBoard = GameBoard();
  const availableMoves = [];

  function placeShipsRandomly() {}
}
